#DB 삭제 생성
DROP DATABASE IF EXISTS jdbc_article_manager; #삭제  ***삭제는 항상 신중하게***
CREATE DATABASE jdbc_article_manager;  # 생성
USE jdbc_article_manager;  #위치 고정


#테이블 삭제  ***삭제는 항상 신중하게***
DROP TABLE IF EXISTS article;
#article 테이블 생성
CREATE TABLE article(
	id INT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
	regDate DATETIME  NOT NULL,
	updateDate DATETIME NOT NULL,
	title VARCHAR(100) NOT NULL,
	`body` TEXT NOT NULL
);

#article 테스트 데이터 
INSERT INTO article
SET regDate = NOW(),
updateDate= NOW(),
title = CONCAT('제목',RAND()), # RAND 난수 
`body` = CONCAT('내용',RAND());

DESC article;
SELECT * FROM article;

UPDATE article
SET upadteDate =NOW(),
title = 'title',
`body` = 'body',
WHERE id = id;

#테이블 삭제  ***삭제는 항상 신중하게***
DROP TABLE IF EXISTS `member`;
#member 테이블 생성
CREATE TABLE `member`(
	id INT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
	regDate DATETIME  NOT NULL,
	updateDate DATETIME NOT NULL,
	loginId VARCHAR(50) NOT NULL,
	loginPw VARCHAR(50) NOT NULL,
	`name` VARCHAR(50) NOT NULL
);
# 테이블 구조확인
DESC `member`;
# 테이블 구조 조회
SELECT * FROM `member`;

#member 테스트 데이터 
INSERT INTO `member`
SET regDate = NOW(),
updateDate= NOW(),
loginId = 'test', # RAND 난수 
loginPw = 'test',
`name` = '철수';

#member 테이블 loginId에 UNIQUE 추가
ALTER TABLE `member` MODIFY COLUMN loginId VARCHAR(50) NOT NULL UNIQUE;


#false = 0 ,true = 1 을 판단하는 로직   join에 쓰고 있음
SELECT COUNT(*) > 0
FROM `member`
WHERE loginId = '13';